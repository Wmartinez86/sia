25 Enero 2012 (Aplicado)
==============
* Quitamos el nombre de Proveedores
* Añadimos el tipo de orden a las cotizaciones
    ALTER TABLE  `o_cotizacion` ADD  `tipocontrata` ENUM(  'o',  'c' ) NOT NULL DEFAULT  'c';
* Se muestra en la lista de cotizaciones el tipo de contratación
* En la lista de órdenes se mejoró la visibilidad de las acciones
* Impresión de cotización
* Impresión de requerimiento

26 Enero 2012
=============
* Cambio de órdenes de compra

27 Enero 2012
==============
* Arreglamos la plantilla de órdenes de compra
* Quitamos el campo siaf en las órdenes de compra y servicio
    ALTER TABLE  `o_ordencompra` DROP  `siaf`; -- Quitamos SIAF
    ALTER TABLE  `o_ordenservicio` DROP  `siaf`;  -- Quitamos SIAF
* Añadimos el campo destino en las órdenes de compra
    ALTER TABLE  `o_ordencompra` ADD  `destino` VARCHAR( 150 ) NOT NULL AFTER  `createdby` ;  -- Ponemos Destino
* Cambiamos la estructura de las órdenes de servicio y sus detalles, haciéndolas igual a las órdenes de compra
    ALTER TABLE  `o_ordenservicio` DROP  `service`; -- Quitamos el servicio de las órdenes
* Añadimos a los detalles de la orden de servicio los campos de cantidad y unidad de medida
    ALTER TABLE  `o_detalleordenservicio` ADD  `cantidad` FLOAT NOT NULL AFTER  `especifica` ,
    ADD  `umedida` VARCHAR( 10 ) NOT NULL AFTER  `cantidad` ; -- Agregamos campos a los detalles del servicio
* Quitamos la tabla o_pecosa
    DROP TABLE  `o_pecosa`; -- Quitamos PECOSA
* Modificacion de requerimiento_print	
* Modificacion de cotizacion_print	
* Modificacion de orden_compra_print	
* Modificacion de orden_servicio_print	
* Aumento del tamaño de la referencia (nrodoc) en compra y servicio
    ALTER TABLE  `o_ordencompra` CHANGE  `nrodoc`  `nrodoc` VARCHAR( 255 ) NOT NULL; -- Tamaño de nrodoc
    ALTER TABLE  `o_ordenservicio` CHANGE  `nrodoc`  `nrodoc` VARCHAR( 255 ) NOT NULL; -- Tamaño de nrodoc

28 Enero 2012 (Aplicado)
=============== 
* Funcionamiento Multisitio
* Creación de la tabla opciones
    CREATE TABLE `o_opciones` (
        `id` int(3) NOT NULL auto_increment,
        `nombre` varchar(100) NOT NULL,
        `descripcion` text NOT NULL,
    PRIMARY KEY  (`id`)
    ); -- Tabla opciones
* Opciones del sistema
    opciones.php
    data/templates/opciones.html
    includes/functions.php
    scripts/common.php

28 Enero 2012
==============
* Actualización de jquery, jquery.ui y jquery.validation

30 Enero 2012
===============
Empezamos con github